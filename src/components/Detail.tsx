import { X } from 'lucide-react';import type { Donation } from '../types';import { formatMoney } from '../utils/data'
const labels:Record<string,string>={ECRef:'EC reference',RegulatedEntityName:'Regulated entity',RegulatedEntityType:'Entity type',CompanyRegistrationNumber:'Company registration number',IsSponsorship:'Sponsorship',IsBequest:'Bequest',IsAggregation:'Aggregation'}
export function Detail({donation,onClose}:{donation:Donation;onClose:()=>void}){return <div className="detail-backdrop" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><aside className="detail" role="dialog" aria-modal="true" aria-labelledby="detail-title"><button className="close" onClick={onClose} aria-label="Close details"><X size={20}/></button><p className="eyebrow">Donation record</p><h2 id="detail-title">{donation.DonorName||'Donor not provided'}</h2><p className="detail-amount">{formatMoney(donation.amount)}</p><div className="ref">EC reference <strong>{donation.ECRef||'Not provided'}</strong></div><dl>{Object.entries(donation.raw).map(([key,value])=><div key={key}><dt>{labels[key]||key.replace(/([A-Z])/g,' $1').trim()}</dt><dd>{value||'Not provided'}</dd></div>)}</dl></aside></div>}
